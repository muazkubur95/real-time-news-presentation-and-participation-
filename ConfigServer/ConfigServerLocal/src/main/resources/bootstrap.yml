spring:
 application:
  name: config-server
 cloud:
  config:
   server:
    native:
     search-locations: classpath:/config
 server:
  port: 8888

# Other configurations specific to the Config Server


